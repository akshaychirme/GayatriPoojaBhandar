// This file contains the business logic of the module. It acts as an intermediary between the repository and the routes/controllers, ensuring that complex operations or transformations are handled here.

const dummyGet = async() => {
	// const dummy = await dummyRepo.get();
	return true;
}

export default{
	dummyGet
}